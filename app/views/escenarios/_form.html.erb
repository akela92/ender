<%= form_for(@escenario) do |f| %>
  <% if @escenario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@escenario.errors.count, "error") %> prohibited this escenario from being saved:</h2>

      <ul>
      <% @escenario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_escenario_ideal %><br>
    <%= f.number_field :id_escenario_ideal %>
  </div>
  <div class="field">
    <%= f.label :id_user %><br>
    <%= f.number_field :id_user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
