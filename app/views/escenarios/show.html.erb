<p id="notice"><%= notice %></p>

<p>
  <strong>Id escenario ideal:</strong>
  <%= @escenario.id_escenario_ideal %>
</p>

<p>
  <strong>Id user:</strong>
  <%= @escenario.id_user %>
</p>

<%= link_to 'Edit', edit_escenario_path(@escenario) %> |
<%= link_to 'Back', escenarios_path %>
